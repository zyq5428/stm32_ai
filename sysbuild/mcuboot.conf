# mcuboot.conf — MCUboot Kconfig 配置
# 启用 USART2 串口恢复（mcumgr SMP 协议）+ KEY0 (PD10) GPIO 按键检测进入

# === 串口恢复模式 ===
CONFIG_MCUBOOT_SERIAL=y
CONFIG_BOOT_SERIAL_UART=y
# 禁用 UART 控制台（避免与串口恢复冲突，USART2 专用于 mcumgr）
CONFIG_UART_CONSOLE=n

# === GPIO 按键检测进入串口恢复 ===
CONFIG_BOOT_SERIAL_ENTRANCE_GPIO=y
# 按键消抖/检测延时 50ms
CONFIG_BOOT_SERIAL_DETECT_DELAY=50

# === 无有效应用程序时自动进入串口恢复 ===
CONFIG_BOOT_SERIAL_NO_APPLICATION=y

# === 日志级别（警告级别节省空间） ===
CONFIG_MCUBOOT_LOG_LEVEL_WRN=y

# === 签名算法 RSA-2048 ===
CONFIG_BOOT_SIGNATURE_TYPE_RSA=y
CONFIG_BOOT_SIGNATURE_TYPE_RSA_LEN=2048

# === 最大镜像扇区数（自动根据分区大小和 Flash 扇区大小计算） ===
CONFIG_BOOT_MAX_IMG_SECTORS_AUTO=y
